{
  "name": "kpoker",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build:blog": "node scripts/build-blog.js",
    "build": "npm run build:blog && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.0.8"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^16.4.2"
  }
}
